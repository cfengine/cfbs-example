bundle agent hello_inventory
{
  meta:
    "tags" slist => { "autorun" };
  vars:
    "hello"
      string => "Hello from CFEngine Build",
      tags => { "inventory", "attribute_name=Hello"};
  reports:
    "Hello from CFEngine Build";
}
